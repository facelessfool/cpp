#include<iostream>
using namespace std;

struct Node {
    int data;
    struct Node * left;
    struct Node * right ;
};


int main(){

    struct * p;//this points to a node
    struct * t;//this points to a node temporarily

}